# mongoDB
- mkdir -p data/db ( <=> mkdir data;cd data;mkdir db)
- mogod --dbpath=./data/db
## 启动操作界面
  1. 用户图形接口GUI
  2. 命令行接口CLI
  （对于mangodb我们使用mongo shell 这个命令行来操作）
## ** 启动mongo shell 的形式是：$ mongo **
- 创建一个数据库 $ digicity-express-api
- 创建一个集合 $ db.createCollection('posts')
